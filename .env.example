# Explicit, isolated preview mode. Never use real customer data in demo mode.
VELLURE_DEMO_MODE=false
# Production: set demo mode to false and connect the existing backend.
VELLURE_API_URL=http://localhost:3000/api/v1

# Sole administrator; must already have SUPER_ADMIN on the backend.
VELLURE_ADMIN_EMAIL=
